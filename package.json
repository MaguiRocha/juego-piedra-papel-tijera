{
  "name": "desafio-final-m5",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "yarn build && yarn deploy"
  },
  "devDependencies": {
    "@parcel/transformer-image": "^2.8.3"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "parcel": "^2.0.0-rc.0"
  }
}
